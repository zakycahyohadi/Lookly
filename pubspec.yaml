name: lookly
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  web: any
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  device_preview: ^1.2.0
  flutter_dotenv: ^5.1.0
  google_fonts: ^6.2.1
  google_generative_ai: ^0.4.6
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: ^0.19.0
  mime: ^2.0.0
  path_provider: ^2.1.5
  flutter_markdown: ^0.7.7
  image_picker: ^0.8.4+4
  image_picker_for_web: ^2.1.4  # Tambahkan package ini untuk Web
  flutter_image_compress: ^2.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.15
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - .env
